---
galaxy_info:
  author: Deutsche Telekom Pan-Net
  description: Ansible role to apply Docker CIS Baseline 1.2
  company:
  license: MIT
  min_ansible_version: 2.8

  platforms:
  - name: Ubuntu
    versions:
      - bionic

  galaxy_tags:
    - system
    - docker
    - security
    - cis
    - hardening

dependencies: []
# List your role dependencies here, one per line. Be sure to remove the '[]' above,
# if you add dependencies to this list.
